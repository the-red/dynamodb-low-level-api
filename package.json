{
  "name": "dynamodb-low-level-api",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1267.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "prettier": "^2.8.0"
  }
}
